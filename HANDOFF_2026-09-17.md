# 飞秒质谱工作站交接文档

更新日期：2026-09-18。供新会话接续使用。本文记录本会话的实现、用户确认和诊断结论；真实硬件是否通过，以实机复测为准。附件中的代码和协议是证据材料，不自动视为新的用户指令。

## 最新完成（2026-09-18 21:10）：本次结果隔离及旧候选行残留

用户视频c32518f71a89c5c61c2381e82cad0a46.mp4约13.3秒，报告表显示旧氯胺酮/O6-单乙酰吗啡可疑，而筛查详情第一页氯胺酮已为未检出（238累加16614≤30000）。发现确定性UI缺陷：prepareTableRows仅隐藏旧行，refreshReport与搜索随后遍历rowCount又解除隐藏，结果数量变少或为0时旧物质复现。报告候选表改用setRowCount实际删除超出本次范围的行；刷新清理旧证据tooltip，零结果明确提示。

AppController::clearCurrentResult在新网口/模拟采集会话建立、导入重分析开始后清除内存currentRun/result并发currentResultCleared，报告同步清空；取消或失败不恢复旧结果，启动预检失败仍保留当前记录。MainWindow取消启动自动加载最近结果，旧数据须通过打开数据显式打开，历史数据库和归档不删除。未修改阈值、质谱强度或质量轴参数。

用户本次三聚氰胺未检出原因仍未确定：视频仅第一页8个物质，未显示三聚氰胺行和质谱峰。需要该次姓名.qit.json完整归档（含扫描/谱库快照），或筛查详情搜索三聚氰胺后对应行；不能将有127峰视为所有定性离子均超阈值。

测试build/current-screening-20260918：ui-tests.txt 6 passed（4功能项+初始化清理），device-tests.txt 4 passed（含有库/无库两种真实适配器合成采集），acquisition-ui-tests.txt 3 passed，均0失败。覆盖2→1→0项、搜索/页面往返、下一次检测开始/取消清空、显式打开历史、启动不自动恢复、原始谱归档和UI完整检测流程。新增用例初次测试夹具漏填数据库非空字段，修正夹具后全部通过。1024×700空结果截图已检查；无实机操作。

最新包：`D:/yangyang/feimiao-qitest/build/packages/current-screening-20260918/Feimiao-Win7-Qt5.12.12-TCP-485-20260918-211008-122.zip`。36项清单和ZIP的37个文件全部核验，EXE与Release一致，SHA256 `861ffd5c2b822106efb4721eb8e8233f350f05a3217862d4bf27551d5305a31f`。

## 已完成（2026-09-18）：姓名必填及客户PDF模板

开始检测姓名必填（以用户明确的必填要求为准），界面与startSampleDetection均拦截空白姓名；检测归档默认姓名.qit.json，报告姓名.pdf，同名加-2/-3等序号，Windows非法字符仅在文件名替换，记录仍保留原名。被检人读取该次检测的person_name。报告采用用户提供“筛查人员1.xz.pdf”版式，四列为序号、化合物名称、定量离子、是否检出，不含浓度；仅列可疑候选并按完整筛查详情再次核对，不列未检出/未筛查。定量离子取本次冻结谱库quantify_ion，无信息显示未提供，不猜测APCI或模板仪器名称。报告页界面原浓度列未改，本次针对导出PDF。无可疑物质明确显示提示。未修改筛查阈值或质量轴系数。

相关测试：WorkspaceTests两项（reportTemplateOnlyListsSuspects、writesTraceablePdf）及UiSmokeTests三个流程（navigationAndAcquisitionRemainStable、customerResultReviewWorkflow、bundledSamplesImportWithoutDuplicates）最终均通过。customer流程旧测试期望3个筛查过滤选项，但现有代码已包含未筛查共4项，同步修正期望。中文PDF实际渲染检查单页可疑/空结果和45项长名5页；小屏录入检查完成。证据在忽略目录build/report-template-20260918。开发机验证不等于Win7实机验收。

最新包：`D:/yangyang/feimiao-qitest/build/packages/report-template-20260918/Feimiao-Win7-Qt5.12.12-TCP-485-20260918-200346-973.zip`。36项清单校验通过，ZIP全部37个文件与展开包一致，EXE与Release一致，SHA256 `f6b29ca2d6dc54a594a9ac1e480496eb80b5e82ac9d1da8d409f6c66dc165389`。

## 已完成（2026-09-18 19:15）：检测停止前等待心跳应答

用户提供18:55～18:57的新抓包，证实两条停止0x15于18:57:02.037/.537发送且被TCP ACK；前一条心跳于02.009发送、02.628才收到应用应答，停止没有应用应答，设备继续上传并报告运行中。电脑05.043超时断开。此前新、旧成功抓包均先收到心跳应答，再隔约0.4秒发送停止。诊断细节和解析产物在忽略目录 `build/stop-failure-20260918/`，不能把TCP ACK当执行成功，不能混同“未检出物质”。

按用户授权修改NetworkInstrument：按本连接实际发送/合法回复计数在途心跳，停止状态禁止新心跳；若有在途心跳，最多3秒等待收齐，再经400ms间隔发送停止。原500ms再发一次、首次停止发送起3秒应答期限不变。无在途心跳直接停止；无心跳应答则明确报停止尚未发送并隔离连接，不无限等、不伪报成功。停止尚未发送时忽略0x15应答，断线/结束清理定时器、重连清空在途计数。方法、485、谱库阈值和强度计算未改。诊断增加pendingHeartbeatReplies、stopWaitingForHeartbeat、stopHeartbeatQuietActive及时间参数/连接事件。

相关验证全部最终通过：`tests.txt`新增8种心跳停止时序及原有停止/数据/方法心跳回归，其中旧连续检测测试因模拟设备丢弃空闲心跳失败，补模拟应答后 `retry-test.txt` 单项复验通过；`controller-tests-final.txt` 4 passed，`ui-tests-final.txt` 3 passed（含初始化/清理）。控制器和UI测试同样补真实心跳应答，覆盖原始谱保存/谱库筛查和1024×700/768气压结束显示。仅内存串口/本机回环，无实机操作；不据此声称固件根因或实机已解决。

最新便携包：`D:/yangyang/feimiao-qitest/build/packages/stop-heartbeat-20260918/Feimiao-Win7-Qt5.12.12-TCP-485-20260918-191525-598.zip`。36项SHA256均核验，ZIP所有文件与展开包一致，EXE与Release构建一致；EXE SHA256 `92428ae4edb578cc67a630be06ab94f5ffcbb1c5e5d24aa5c2cb66c7c4c76cad`。包包含下面的85±2℃改动。下一步实机连续检测核验，若失败保留本次网口JSON和pcapng。

## 已完成（2026-09-18）：开始检测的离子阱温度范围

用户要求开始检测允许85±2℃。AppController::checkDetectionStart现在要求有效有限实测值83.0≤T≤87.0℃，两个边界均可开始；超出或无有效回读时弹窗列出83.0～87.0℃，只有确认按钮。当前方法成功回执及真空条件保持；一键开关机温度目标和流程未改。

相关测试日志在 `build/detection-temperature-20260918/`：controller.txt 4 passed、ui.txt 3 passed，0失败（含初始化/清理）。验证82.9/87.1拒绝，83/84.9/85/87通过，并分别在83和87℃完成合成网口检测、原始谱保存及谱库筛查。尚需实机复测。

最新包：`D:/yangyang/feimiao-qitest/build/packages/detection-temperature-20260918/Feimiao-Win7-Qt5.12.12-TCP-485-20260918-163508-800.zip`，包含下面已完成的质量轴校准。

用户另报真实样品“检测未完成”、截图23.0秒。有部分质谱不代表完成；如果设置20秒，优先核对停止回执3秒超时，但尚无本次日志，不能认定原因。已请用户在不重启/重连/再次检测前导出网口JSON及有断线时的485报文，并提供检测时间、是否刚同步校准。温度范围调整不代表已解决该采集失败。

## 已完成（2026-09-18）：手填质量轴二次校准

用户已回复“按这个理解来”：同步更新后续方法下发和质量轴反算共用系数，不只是改显示。已完成 Fullscan 手填二次校准，至少3组不同的实测/理论m/z。入口：设置与维护→分析校准→质量轴校准；填写→拟合校准→同步校准→重新设为当前方法，待全部成功回执后才能检测。

计算由 `src/core/FullscanCalibration.h` 实现：先用旧 a/b/c 将实测m/z还原扫描电压，再用理论m/z→扫描电压做归一化QR二次拟合。所得新 a/b/c 同时用于 NetworkProtocol 方法编码和采集轴反算。不是把m/z→m/z的拟合系数直接当作扫描电压系数。验证有限值、正数、点序/重复、0..801 m/z范围内单调性及编码范围；校准点以外标明外推。

同步通过 AppController 原子保存 `fullscan-calibration.json`（应用数据目录；测试指定 QITEST_WORKSPACE_DB 时在该数据库旁），保存current/previous、支持重启恢复及撤销上次同步。无文件用原 datafit 默认系数；损坏文件阻止方法下发，不静默退回默认。同步使旧方法确认失效，忙碌/采集中不许同步；不会自动给设备发命令。成功后清空实测列，避免旧数据重复校准。新记录保存使用的系数，历史记录保持原轴。当前仅内置网口/485组合适配器接入，离线可保存，不猜厂家额外同步命令。

最新交付：`D:/yangyang/feimiao-qitest/build/packages/mass-calibration-20260918/Feimiao-Win7-Qt5.12.12-TCP-485-20260918-134349-544.zip`，包含下节泵启动门槛及10秒等待修改。已核验36项SHA256，构建/展开包/ZIP中EXE一致（SHA256：AAAD022724AC996C17F8FC460DFAF48F33812609D59B87DB4AD65812262E45E5）。

相关测试 `build/mass-calibration-20260918/`：core.txt 3 passed、network.txt 12 passed、controller.txt 3 passed、ui.txt 6 passed，均0失败（含初始化/清理）。覆盖237→238、方法编码与反算一致、同步使旧确认失效、采集中拒绝修改、保存/重启/撤销/损坏文件，以及1024×700/768界面。截图在该目录captures，已检查；这些是合成数据和本机回环测试，尚需Win7实际标准样复测。用户未要求子代理，不使用。

## 最新进展（2026-09-18）：分子泵启动门槛严格小于8 mbar

本节优先于下文历史真空门槛；交付包以最上方质量轴校准节为准。用户最新确认：隔膜泵开启→真空度＜8E0 mbar→分子泵开启，分子泵开启后的真空规则不变。StartupSequence::molecularPumpStartPressureAllowed现为有效有限值且0<p<8.0；等于8不放行。一键启动和网口适配器手动开启共用该门槛，等待/拒绝提示同步。启动准备期仍确认隔膜泵及TD设定；载气>0时最终p<0.01、载气=0时p<0.0001的规则保持，10秒泵启动确认及约0.5秒查询节奏保持。

最新交付：`D:/yangyang/feimiao-qitest/build/packages/pump-vacuum8-20260918/Feimiao-Win7-Qt5.12.12-TCP-485-20260918-112010-493.zip`。36项SHA256和构建/展开包/ZIP中EXE一致。相关测试在`build/pump-vacuum8-20260918/`：pump.txt 6 passed、network.txt 7 passed，0失败（含初始化/清理），覆盖8两侧相邻浮点数、等于8、7.99/8.01网络回读边界、后续动态载气真空规则、10秒启动等待及保持连接。本机合成测试，不代表Win7实机验收。

同日最新附件485报文.json（文件修改时间11:06:52）的实际证据：11:03:35.150发泵开，11:03:40.907确认成功（约5.76秒）；11:04:22.262发泵关，11:04:22.565确认关闭。导出connected/open=true、lastFailure为空；仅保留11:04:34至11:04:49原始记录，不能据此证明用户反馈的掉线原因。该附件没有网口真空历史，已告知需同次网口报文和异常时自动485日志。此次门槛修改不能宣称断线问题已全部解决。

## 历史进展（2026-09-18）：分子泵开启确认延长到10秒

本节优先于以下所有历史状态。用户要求等待分子泵开启10秒，查询仍约0.5秒。已将Rs485Instrument的molecularPumpOn=true确认期限改为10000ms（从请求入队开始），使用精确定时器；其他控制仍4500ms，查询间隔/主板与310交替/1500ms单次查询超时不变。AppController手动开启的宿主保护延长到11秒，避免原5秒提前取消；一键启停原本由设备步骤计时。有效电流>0才算开启，满10秒仍有有效0 A回包时停止后续启动并保持485。真实无回包仍按通信故障处理；不自动重发，不保证实际泵故障已解决。

最新包：`D:/yangyang/feimiao-qitest/build/packages/pump-wait10-20260918/Feimiao-Win7-Qt5.12.12-TCP-485-20260918-104941-148.zip`，替代以下历史包。已验证36项SHA256及构建/展开包/ZIP中EXE一致。

相关测试日志：`build/pump-wait10-20260918/`，pump.txt 8 passed、network.txt 14 passed、controller.txt 7 passed，均0失败（含初始化/清理）。覆盖约6.9秒启动成功、满10秒零电流及在途半帧、手动启动越过5秒、无回包故障和关机降温/降速原规则。均为合成串口及本机回环TCP，尚需Win7实机复测。

## 历史进展：三份自动日志证实泵电流为0，软件控制期限主动断口

本节优先于后面的第0节及其他旧状态。用户随后提供以下文件（均在此前微信目录 `D:/杨扬/Documents/xwechat_files/Yangyang975696339_3669/msg/file/2026-09/`）：

**最新交付ZIP**：`D:/yangyang/feimiao-qitest/build/packages/pump-zero-current-20260917/Feimiao-Win7-Qt5.12.12-TCP-485-20260917-211049-710.zip`。已核验36项清单及构建/展开包/ZIP中exe一致，替代下文此前的所有包。新增开关能力断言回归在`build/pump-zero-current-20260917/buttons.txt`，4 passed，0 failed（含初始化与清理）。

- `485-20260917-130106-899-e7aeb425.json`
- `485-20260917-130155-904-5f5461fc.json`
- `485-20260917-130227-908-6ad3976f.json`

三次开机均已完成外载气模式、隔膜泵开启、TD250的ACK/回读。分子泵启动TX分别为北京时间21:01:02.394、21:01:51.655、21:02:23.389，全部为用户指定的 `0011001006111111015\r`。后续RX有同文回显，但不当作泵已启动。每次启动后均有9个完整310电流回复，数据全为000000（0 A）；主控板30与310交替查询且持续正常回复，证明上一版主控板防失效修复已经生效。

真正失败均为4500ms控制确认期限（从排队请求开始计时，故发送到失败可能少于4.5秒），reason=“控制回读未确认，已停止后续步骤；设备实际状态请核对”。失败前最后主板读数仍新鲜，不是主板5秒读数过期，也不是COM选错或USB断连。第一份启动前留存转速约1648降至995rpm，后两份启动前留存转速0。启动等待期原实现主要查310，无后续转速证据，不能仅由这些记录确定实际泵不启动的硬件原因，也不能保证延长等待就能启动。

本轮修复：Rs485Instrument控制期限到时，若仍有新鲜主控板及泵电流回读，但电流未达到启停目标，则失败结束该控制、停止后续开机而保留485与有效监控。提示“分子泵启动未确认：电流仍为0 A；已停止后续开机步骤，485保持连接”。正常轮询随后恢复，包括转速，便于查实际启动延迟。期限到时若主控帧接收一半，保留解析缓存、pending和查询时限，不抢发新请求。真正查询无回复、主板失效等仍保留原断口策略；不延长4.5秒、不修改启动命令、不自动重发、不用回显/零电流报成功。自动异常快照也支持保持连接的控制失败，保存真实读数。

相关回归日志：`build/pump-zero-current-20260917/network.txt`（16 passed）、`pump.txt`（8 passed）；新增zeroCurrentTimeoutKeepsConnectionAndPendingReply模拟命令回显、持续0 A、期限时半帧在途、后续电流变正只更新监控不复活原失败操作。NetworkTests覆盖不升离子阱、连接保持、继续/关机能力恢复及温热设备重新连接后续开机。仍待实机验证，当前重点是确认泵在启动命令后为何持续0 A，不能将“保持串口连接”称为已解决实际泵启动。

## 0. 最新补充：一键开机断485与重连后的继续开机（优先于下文旧状态）

用户随后提供 `D:/杨扬/Documents/xwechat_files/Yangyang975696339_3669/msg/file/2026-09/485报文(2).json`，本轮已修改代码并回归，等待新版实机验证。不是前一份COM1完全无RX的问题。

新日志：COM4已连接且持续有效回读。保留上次失败时间20:40:21.720，controlKey=molecularPumpOn、controlStage=4、activeQuery=1、pendingQuery=false、lastReadback为空，原因只有“控制确认已取消，设备状态请核对后重新连接”。20:40:51.542已有一键关机heatingOn=false成功，20:44:23.833发送分子泵停止；20:45:49回读电流0、转速9813rpm，尚在降速，隔膜泵仍开。导出时不是仍然断开，不能把降速过程不许重新开机当成全部都是UI错误。

异常当时自动日志路径在用户电脑：`C:/Users/Administrator/AppData/Local/宁波新芝生物科技股份有限公司/飞秒质谱工作站/diagnostics/485/485-20260917-124021-731-d355b3a2.json`。当前附件是重连后的导出，旧控制历史已清除，因此现场是否完全同因仍须原始自动日志或复测确认。

已本机复现一个相符的软件缺陷：TD回执延迟1.2秒（在合法时限内），随后分子泵电流3.9秒才变为正，旧逻辑等待电流时只查310，主控板超过5秒未查询，被判失效，NetworkInstrument取消开机并关闭串口。测试修改前失败且上层原因“485状态失效”；修改后通过。

修正：

- Rs485Instrument等待泵电流期间，在主控板30与泵310之间交替查询，保持200ms静默和原4500ms控制时限；真正不回包/不满足目标仍失败，不无限重试。关机等转速归零仍采用原轮询流程。
- NetworkInstrument本次开机失败后重连，双泵实际运行且温度设定未确认，即使当前余温已达标也允许用户点击“继续开机”；只补温度，不重启泵/切换载气，不自动继续。健康设备重开且无中断时的防重复启动仍保留。
- Rs485Instrument增加带原因的取消入口；上层取消保存真正触发原因到自动日志，不再全部覆盖成泛化取消提示。

回归日志 `build/startup-recovery-20260917/`：network.txt共18 passed，pump.txt共10 passed，ui.txt共7 passed，均0失败（含测试初始化/清理）。新增NetworkTests::slowPumpStartupKeepsBoardFreshAndCanResume，UI继续开机新增interrupted-warm-reconnect数据行；1024×700截图已检查。本轮没有改真实阈值、CRC、流速倍率或控制编码。最新包见本节末补充。

本轮最新包（替代第3节的旧电压包）：`D:/yangyang/feimiao-qitest/build/packages/startup-recovery-20260917/Feimiao-Win7-Qt5.12.12-TCP-485-20260917-205438-968.zip`。已校验SHA256清单36项及构建/展开包/ZIP中exe一致。未进行真实设备操作，待实机复测。

## 1. 接手先看什么

1. 阅读本文件、`START_HERE.md`、本次受影响模块的 README 和测试。
2. 保留当前全部暂存及未暂存修改，不 reset、不回退、不覆盖其他功能。当前工作尚未统一提交。
3. 最新进展见第0节；第2节COM1无回包是较早的一次记录，不应再用于解释当前COM4运行中断线。
4. 另有两个未闭环问题：载气设定1而回读约4；新软件气压曲线仍平。不能用修改显示数值、制造波峰等方式“修复”。

项目：`D:\yangyang\feimiao-qitest`。

工作约定：只维护飞秒质谱工作站；C++17 / Qt 5.12.12 / Qt Widgets / CMake；正式交付 Windows 7 SP1 / MinGW 7.3 x64 / 8 GB；界面验证1024×768及任务栏占用后的1024×700。主流程为“方法选择 → 样品分析 → 报告查看”。科学计算确定性实现，不猜协议、时间轴、谱库判定。客户报文、抓包、构建缓存和安装包不提交Git。内部历史目录名 scientz 不代表另一个项目。

## 2. 最新问题：换机器后485连接失败

用户提供：

`D:/杨扬/Documents/xwechat_files/Yangyang975696339_3669/msg/file/2026-09/485报文(1).json`

已实际读取，关键事实：

| 项目 | 记录 |
| --- | --- |
| 串口 | COM1 |
| 查询时间（北京时间） | 2026-09-17 20:29:42.201 |
| 查询TX | `55 88 30 00 01 01 AA`，主控板状态查询 |
| 接收 | receivedBytes=0、parsedFrames=0；无RX记录 |
| 失败时间 | 20:29:43.707，约1.506秒后 |
| 原因 | 485查询超时，读数已失效；请检查设备后重新连接 |
| 状态 | connected=false、open=false；controlHistory和confirmedSetpoints为空 |

结论：这次从第一次查询起就没有回复，尚未下发方法参数；不能据此核对“设1显示4”。TX记录代表软件发送尝试，不证明下位机收到。之前机器常用COM4，此次COM1，串口选择是优先排查项，但尚不能认定选错。

已向用户说明的操作：设备管理器 → 端口（COM和LPT），拔插USB转485适配器，找到实际消失/出现的COM号；软件选择实际端口，退出旧软件等串口占用程序后连接。若仍无回复，再查驱动、接线和下位机供电。不要盲目指定COM4，也不要仅延长超时掩盖无回包。

日志的 pumpEnabled=false 不足以解释主控板无回复，不能把它当作此次根因。用户“换机器”可能指电脑或设备，尚未进一步确认。

## 3. 最新可交付安装包

已生成并核验的最新便携ZIP：

`D:/yangyang/feimiao-qitest/build/packages/method-voltage-20260917/Feimiao-Win7-Qt5.12.12-TCP-485-20260917-195108-379.zip`

同目录有同名展开目录。该版本含此前485诊断、启停、气压连续曲线、就绪显示及最新离子源电压0x50修改。

已核验36个清单文件，构建程序、展开包和ZIP内程序哈希一致。最终硬件验收未完成，不能将源码编译和模拟通讯测试等同于实机通过。

最新电压修改测试日志：

- `build/method-source-qa/network.txt`：11 passed，0 failed。
- `build/method-source-qa/rs485.txt`：5 passed，0 failed。

## 4. 未闭环：载气设置1，显示约4.13 mL/min

截图中方法载气流速1，仪器监控4.13；监控值来自设备回读，不是直接显示方法设定。

已核实当前实现：

- 写入：485 `0x12`，2字节大端，范围0～50 mL/min，值乘10。设1应发送数据 `00 0A`，帧为 `55 88 12 00 02 00 0A AA`。
- 读取：`Rs485Protocol.cpp`，状态数据偏移19的U16除200，赋给 `carrierGasMlMin` / `carrierGasFlowMlMin`，界面显示该回读值。
- 用户曾明确给出写入乘10，优先于旧PDF中的乘1000。不得未经证据改回PDF倍率。
- 回读除200的换算尚不能凭设1显示4就判错；控制ACK也不等于流量已稳定达到设定。

待做：取得成功连接后、点击“设为当前方法”前后的一份新485日志，核对0x12是否确实下发、数据和ACK，再核对原始回读。最新COM1日志没有任何RX，不能用于换算判断。本轮没有为此修改代码。

## 5. 未闭环：新程序气压图平，旧程序有周期波峰

### 已实现的正确绘制方式

用户提供旧代码并确认：period_ramp_waveform_time取方法页面“周期”；gas_pressure_time取已下发方法数组data[8]。

```text
x[n] = n / 1000 / 60 * (period_ramp_waveform_time / gas_pressure_time * 2)
y[n] = 大端U16(帧偏移7 + 2*i) / 65535 * 2.5 * 5.7
```

时间轴为min。直接使用方法原始周期参数，不能擅自固定每周期1秒。例如周期10000、data[8]=5000，单点间隔4ms。

当前改为保留全部0x82周期、连续拼接；新检测清空，完成后保留整段；有效方法确认后固定采样间隔。纵轴0～6V。不合成波峰、不平滑成旧图。未知时间参数时显示采样点，不伪造时间。保留上限100万点并有显式标识。该曲线尚无历史归档功能。

### 抓包证据

最新旧软件抓包：`D:/杨扬/Desktop/old.pcapng`。

分析目录：`build/old-pressure-20260917/`，有 `对比结论.md`、`pressure-summary.json`、`method-differences.json`、`old.json`。

| 数据 | 周期 / 点数 | 原始ADC | 按同一公式电压 |
| --- | --- | --- | --- |
| 最新旧程序抓包 | 21周期，0～20；5229点 | 2816～20516 | 0.612314～4.461021V |
| 新程序此前报文 | 22周期，0～21；5478点 | 2816～2888 | 0.612314～0.627970V |

已做TCP去重重组，相关旧软件数据流无缺口、冲突或未解析残留。新报文原始接收块也包含对应完整帧。因此在这两份数据中，平直差别已经存在于原始ADC，不只是纵轴或绘图丢点。

重要：旧、新方法有多个不同字段，截图还存在内/外载气模式差别，非完全相同实验条件。不能断言某一个命令就是根因。旧抓包发现0x50电压设置为190，新版此前发0；现在已修复按方法下发，但是否解决气压波峰仍需同条件实测。

旧方法部分差异槽位（旧/新）：5=119/92，6=207/122，7=1616/892，9=900/325，10=652/549，11=704/579，12=500/590，13=410/380，22=1300/1000，33=652/651，34=673/674。周期10000、data[8]=5000相同。不可直接覆盖整套用户方法或校准值。

其他资料：`build/pressure-flat-review-20260917/summary.json`、`pressure.bin` 为新程序平直数据；更早 `D:/杨扬/Desktop/new.pcapng` 实际是旧软件有峰的一次运行，分析在 `build/pressure-new-review/`，29周期7221点，勿仅凭文件名判断新旧。

分析脚本：`build/pcap-compare/analyze.py`，使用时先读脚本并设置独立输出目录。此前尝试matplotlib绘图失败（模块不存在），没有产出该对比图。

## 6. 最近已完成：离子源电压按方法走网口0x50

用户确认旧软件实际设3800V；截图3788.3V是回读。后续明确3500V也走网口0x50。

旧抓包3800V实际帧（三次发送、每次回执）：

```text
TX 55 10 50 00 03 01 01 BE F5 D6 AA
RX 55 10 50 00 03 01 01 11 89 96 AA
```

实现为电压/20编码U8：3800→BE，3500→AF，0→00，5000→FA。允许0～5000V、20V整数步进；非有限数、越界、无法精确表达的步进报错，不静默取整。

- `NetworkProtocol::ionSourceVoltageCommand()` 生成帧。
- `NetworkInstrument` 在任何写入前校验，然后以方法source值发送三次0x50并逐次等ACK；三次成功才完成方法设置并恢复心跳。
- 删除485方法校验中“离子源电压必须保持0”的旧阻断。485不负责此参数写入，也不冒充已确认该电压。
- 实际电压回读仍来自485状态值除10，例如37887→3788.7V，不用设定覆盖。
- 用户确认TD设0表示“把0作为温度设定值下发”，已保留485 `0x02` 数据0000，不改成默认250或“不下发”。

相关测试：`ionSourceVoltageMatchesCaptured3800V`、`legacyMethodFollowupsRequireAllAcks`、`heartbeatPausesAcrossSerialAndNetworkMethodThenResumes`、485基本参数失败/零值回归。README和打包说明已更新。

## 7. 最近已完成：常用部件“系统状态”显示

用户期望真空、腔内温度、TD温度三个条件达标时显示“已就绪”。原卡片复用包含方法状态的health.ready，导致旁边提示达标而卡片未就绪。

已用独立 `NetworkInstrument::operatingConditionsReady()` 驱动此卡片：

- 连接/回读有效，隔膜泵和分子泵状态满足既有真空运行判断。
- 有载气时0＜p＜0.01mbar；无载气时0＜p＜0.0001mbar。
- TD在220～280℃，离子阱在80～90℃，边界包含。
- 显示“已就绪/未就绪”。不以是否设过当前方法作为此卡片条件。

“开始检测”的门槛与此卡片分开：方法下发成功确认、有效真空达到E-03及更低、离子阱严格＞85℃；未满足时弹窗列出原因，只有确认按钮。

固定开机温度范围仍为用户之前指定的250±30和85±5；TD方法0的确认不等于用户授权把该范围重新解释为0附近。

相关UI测试 `readinessUsesLiveVacuumAndTemperaturesWithoutMethod` 及电源状态回归已过，已看1024×700截图。与最新电压包合并交付。

## 8. 485断开、自动诊断和启停控制

此前运行中/点关机断485：日志提示泵停止命令后约45ms就发下一查询，泵ASCII回复混入，随后查询超时。

已改：泵启停后等待200ms连续串口静默；每段新RX重置静默计时，期间保留记录不当作新主板查询回复。总计1500ms持续噪声则失败并停止后续动作。不能假设泵已成功，也不自动反复发启停。

主板查询超时当前1500ms。共享COM采用顺序通讯，不另开同一COM。

自动异常日志路径：

```text
%LOCALAPPDATA%\宁波新芝生物科技股份有限公司\飞秒质谱工作站\diagnostics\485
```

保留lastFailure、控制历史和RX；异常关闭串口前落盘，重连不清掉磁盘文件。controlHistory最多96条，独立于256条轮询环形记录。手动断开不当异常。

界面支持断开后/无报文时导出，能打开异常记录目录；导出为空也有明确说明。最新用户COM1日志已经包含实际自动保存路径，说明诊断落盘可用。

## 9. 用户确认的硬件协议与顺序（不要重新猜测）

### 一键开机

1. 隔膜泵就是用户最早说的离心泵。
2. 隔膜泵启动与TD加热同时进行；TD目标250℃。
3. 分子泵启动条件用户明确纠正为 **E+04，正04，不是E-04**。当前实现0＜p＜1e5mbar；不要自行改回负04。
4. 分子泵开启后，载气流速回读＞0判载气开；0判关。内/外模式都不等于载气开关。默认外载气。
5. 有载气真空E-03及更低达标（p＜1e-2）；无载气E-05及更低（p＜1e-4）。运行中切换流量也重评。
6. 真空系统完成后才开始离子阱85℃升温。

软件重开后根据实时泵电流、转速、主板状态等恢复显示，不自动重启泵或自行发加热指令。运行中温度未完成时有继续开机逻辑；不能仅根据软件内旧布尔值判断硬件开关。

### 一键关机

点击关机立即关闭TD加热及离子阱加热（48508/02）。等待实际离子阱温度严格＜75℃，再停分子泵；等待新回读电流0且转速0rpm，最后关隔膜泵。不用统一5秒超时替代降温和降速。

### 常用控制与单位

| 控制/数据 | 协议 |
| --- | --- |
| 分子泵开 | ASCII `0011001006111111015\r`，仅CR结尾 |
| 分子泵关 | ASCII `0011001006000000009\r` |
| 泵转速查询 | `0010039802=?115\r`，398，6字符数值直接rpm |
| 泵电流查询 | `0010031002=?099\r`，310，原始/100 A；＞0认为开，0认为关 |
| 泵电压查询 | `0010031302=?102\r`，313，原始/100 V |
| 泵温度查询 | `0010032602=?106\r`，326，直接℃ |
| 泵回复取值 | mid(5,3)指令，mid(10,6)数值 |
| RF电源 | 485 0x10 |
| 离子源HV_24V电源 | 485 0x09；不同于设定离子源电压的网口0x50 |
| 485上述开/关 | 01/02，按ACK和状态回读确认 |
| 隔膜泵 | 485 0x11，开01/关02 |
| 夹管阀/放空 | 网口0x22，开22/关23；未确认时状态未知 |
| 载气模式 | 4850x03，仅内外切换，默认外载气01 |
| EFC设定 | 4850x12，U16BE，mL/min×10 |
| EFC回读 | 状态数据偏移19，U16/200 mL/min |
| TD设定 | 4850x02，U16BE，250→00FA，0→0000 |
| 离子阱设定 | 4850x13，85→0055 |
| 温度回读 | /10 ℃ |
| 网口开始/停止检测 | 0x15，22/23 |
| 网口心跳 | 当前实现0x30，payload23，每2秒 |

心跳特别说明：用户早期说可继续22，但后续要求按旧软件发包修改，当前代码已依据旧包改为23（本次读取源码确认）。新会话不要仅截取早期一句又改回22。方法设置前暂停，完整成功后恢复。

网口数据0x81/0x82：CRC只记录不阻断，仍按55起始、长度、AA结尾拆帧；控制/状态回复维持对应校验。不能把早期“55开头都收”解释为不校验长度且任意噪声入库。周期字段U16BE，从0开始每周期加1。

协议文本缓存：`build/protocol-recheck-0910/便携式质谱485通讯协议.pdf.txt`、`build/protocol-recheck-0910/质谱网口通讯协议.pdf.txt`。遇到与用户明确校正冲突，以用户已确认内容为准，并核对实包。

## 10. 谱库、EIC、筛查和报告的既定语义

- 参考谱库已有“我的谱库”、新建/导入.lib、编辑、保存/另存为/导出、物质增删改等流程，使用用户指定库路径中的.lib。
- 物质包含名称、CAS、母离子、定性离子、定量离子、样品类别、是否内标、一级/二级阈值。兼容旧库字段，不因旧文件表示差异误判全部无效。
- EIC每点是该帧选中m/z窗口的丰度值，不画逐帧累计值；横轴min，20秒实际为0.3333min，不能裁成精确0.3min丢数据。
- 检测筛查时对每个定性离子，在MS1各帧±0.5Da窗口内取强度并跨帧累加；每个离子分别与对应一级阈值比较。
- 定性离子数量不固定三个，也可以一或二；阈值数量必须匹配。
- 必须所有离子的累加值分别 **严格大于** 对应阈值才判“可疑”；等于不通过，包括0不大于0。
- 无效配置为“未筛查”，有效但不满足阈值为“未检出”；不混淆成完全没有采集信号。
- 报告查看的“可疑结果”只列可疑项；筛查详情显示完整物质和累加/阈值。已完成且主表空通常表示本次无符合规则的可疑项，不是写死空页面，也不能推断绝对不含任何物质。
- 本次检测使用开始时指定库快照，避免事后库修改影响已完成记录。

## 11. 源码入口与验证方法

| 工作 | 入口 |
| --- | --- |
| 网口方法、心跳、真空就绪、气压拼接 | src/device/NetworkInstrument.cpp / .h |
| 网口帧与0x50换算、气压时间 | src/device/NetworkProtocol.cpp / .h |
| 串口轮询、方法参数、控制等待、自动诊断 | src/device/Rs485Instrument.cpp / .h |
| 状态回读换算 | src/device/Rs485Protocol.cpp |
| 分子泵ASCII | PumpProtocol、PumpReader |
| 启停顺序 | src/device/StartupSequence.h、ShutdownSequence.h |
| 控制目录 | src/device/VendorControlCatalog.h |
| 控制入口/导出 | src/app/AppController.cpp / .h |
| 常用部件和就绪卡片 | src/ui/MainWindow.cpp |
| 气压曲线 | src/ui/DeviceWaveformPanel.cpp |
| 串口页与异常日志入口 | src/ui/Rs485ConnectionPanel.cpp |
| 回归 | tests/NetworkTests.cpp、Rs485Tests.cpp、UiSmokeTests.cpp、InstrumentControlTests.cpp、PumpTests.cpp |

开始改动前阅读对应README和受影响测试。只运行相关测试，不主动广泛重构。当前Git既有暂存也有未暂存修改，涉及上述文件、README、集成说明和打包脚本；本交接文档不表示这些改动已提交。

本机工具链：

```text
Qt bin: D:\Qt\5.12.12\5.12.12\mingw73_64\bin
MinGW bin: D:\Qt\5.12.12\Tools\mingw730_64\bin
CMake: D:/Qt/Tools/CMake_64/bin/cmake.exe
Python: C:/Users/admin/.cache/codex-runtimes/codex-primary-runtime/dependencies/python/python.exe
测试构建: build/library-import-qa
发布构建: build/win7-qt51212-release
```

打包使用 `scripts/build_win7_qt51212.ps1 -OutputRoot <独立绝对输出目录>`，保留脚本UTF8 BOM及CRLF。交付前核验SHA256SUMS及构建/打包/ZIP中exe一致。

Qt测试用 `-o <日志路径>,txt` 输出，避免控制台重定向空日志。UI可用QT_QPA_PLATFORM=offscreen和相应Qt平台插件路径；QITEST_UI_CAPTURE_DIR用绝对目录。测试模拟串口不得访问真实设备。检查git diff --check；客户数据继续放忽略的build下。

## 12. 新会话推荐下一步

当前优先验证第0节的新开机修复；如果仍然断线，索取异常当时自动保存的JSON，它保留重连前控制历史和具体失败原因。COM1无回包问题只适用于第2节旧记录。载气流速仍需单独抓取0x12下发及回读。

气压问题另取最新安装包下、与旧软件相同方法和载气条件的抓包，比较0x50等控制指令和0x82原始数据；保留“原始数据平”与“绘图错误”的证据区分。

如用户要求新修改，沿现有工作区继续；不要重新实现已完成的谱库、筛查、启停和就绪功能，也不要把所有历史诉求视为本轮待办。
