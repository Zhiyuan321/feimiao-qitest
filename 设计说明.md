# QITest Qt/C++ Design Contract

本工程使用 Qt Widgets-first 的 SCIENTZ 4S 视觉基线，但页面流程和功能层级以飞秒质谱仪 Word 文档 2.3 为最高产品依据。详细参考绑定见 `design/界面交互参考.md`。

- Word 2.3 是页面、功能和层级的上位依据。
- 使用 Instrument Console Profile：顶部命令、中央 TIC/MS、右侧遥测、底部状态。
- 指定色：新芝青绿 `#00A8A8`、警告橙 `#E19D00`、中性灰 `#707372`。
- Filled Primary 使用 `#007A7A`，确保白色文字可读。
- 科学数值由 C++ 确定性引擎计算；AI 不可修改正式数值。
- 不得把只读遥测做成输入框，不得混用旧位图和新图标，不得强加与 QITest 不符的左侧栏。
- 全局视觉只通过 `ScientzTheme` 和应用级 QSS 设置；所有用户命令通过 `ActionRegistry` 注册。

## 增量补强原则

当前 Qt Widgets/C++ 架构、页面层级和确定性数据引擎保持不变。后续设计采用
“Quiet UI, Strong Data（界面安静，数据突出）”作为审查原则，只补强真实缺口，
不以现代化为理由进行大规模重构。

1. **操作层级**：每页只保留一个清晰的主要操作；次要操作使用普通按钮，低频操作使用无底色按钮或二级入口。
2. **表格细节**：统一整行选择、悬停反馈、行高、数字对齐和完整内容提示；只有现有控件无法满足稳定需求时才迁移到 `QTableView + Delegate`。
3. **谱图交互**：优先完善悬停定位、峰值提示、局部选择和缩放反馈，不用装饰动画掩盖数据状态。
4. **AI 上下文联动**：智能台统一提供解释、跳转、执行和确认四类结果；模型只提出受约束意图，执行继续经过 C++ 白名单、权限和风险门控。
5. **原生行为**：按钮、输入、选择、滚动和焦点继续使用 Qt 原生控件行为，保留键盘操作、DPI、Windows 兼容性与可访问性。
6. **响应式侧栏**：智能台与仪器栏可独立开关并可同时使用；窗口不足时优先保证中央数据区最小宽度，再压缩或收起侧栏。

补强顺序为：操作层级 -> 图表交互 -> 表格细节 -> AI 上下文联动 -> Windows 一致性验证。
