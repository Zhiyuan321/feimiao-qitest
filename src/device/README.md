# 厂家工程师从这里接入

2026-09-18：分子泵开启确认期限从请求入队起延长到10000ms，停止及其他控制仍为4500ms，关机等转速归零仍无总期限。查询节奏不变：310与主板30交替查询、200ms间隔，正常回包时约0.5秒检查一次泵电流；单次无回复1500ms仍按通讯故障处理。10秒内以有效电流>0确认开启；有有效回包但持续0 A时停止后续开机、保留485连接。

2026-09-18停止时序兼容：现场失败抓包两条停止0x15均被TCP确认，但发出时心跳0x30尚未应答，下位机仅回复心跳并继续采集；两个成功样本在心跳回复后约0.4秒才发送停止。现停止请求先禁止新心跳，若仍有本连接未应答心跳则最多等待3秒收齐，经400ms间隔后才发送停止；没有在途心跳则直接发送。收到状态、TCP ACK、半帧或未发送停止前的0x15 ACK均不解除等待/确认完成。原停止500ms重发一次和首次停止发送起3秒应答期限不变；心跳等待是独立阶段，无应答明确报“停止指令尚未发送”并隔离连接。断线/完成清除等待定时器，重连清空在途计数；仍须真实停止成功回复及完整质谱才完成。诊断新增在途心跳计数、停止等待/间隔状态及连接事件。此修改根据抓包调整兼容时序，不代表已证实下位机内部丢指令原因，需实机多次检测验证。

## 一键开机与分子泵控制（2026-09-17）

2026-09-17三份21:01～21:02自动日志确认另一种情形：主板回读新鲜、泵310连续回复000000，每次9条，4.5秒控制期限到时软件主动断口。现区分“有有效回包但泵目标电流未达到”与“通讯失败”：前者结束本次控制并停止后续开机，保留485、实际读数、正常轮询和自动异常快照；不重发启停、不假报成功。期限到时在途查询保留pending、解码缓存和单次查询超时，完整收完后才发下一查询；迟到的正电流只更新实时状态，不复活失败的开机流程。无有效电流、主板已失效、串口错误或查询无回复仍沿用断口规则。该修改不证明泵实际启动问题已解决。

2026-09-17开机掉线回归修正：分子泵启停电流未达到目标时，原流程仅反复查询310，可能使主控板5秒读数失效并取消开机、关闭串口。现保留200ms静默和原控制时限，在等待电流阶段交替查询主控板30与泵310；主板每次必须有真实回包，不能用泵回包刷新主板有效期。长时间关机降速仍按原顺序轮询，等待电流及转速均为0。开机中断后重连，若双泵实际运行但温度设定未确认，即使余温达标也允许用户明确点击继续开机，仅补温度步骤；健康运行且无中断仍禁止重复开机。取消控制时自动异常日志保存上层具体原因，不再一律覆盖成“控制确认已取消”。本地已复现并回归，现场同因仍需异常时自动日志核对。

2026-09-17共用485启停间隔修正：用户报文显示分子泵停止后45ms即查询主控，随后出现非有效帧并被控制查询超时关闭串口，疑似启停响应与后续查询争用总线。泵启停后采用现有轮询的200ms静默间隔；期间接收只保留诊断、不确认状态，每个新字节块重新计静默间隔，1500ms仍未安静则失败，不无限等待、不重发启停。之后仍以新查询电流/转速确认。该修正不能证明其他无记录断线同因。

异常关闭串口前记录原因、UTC时间、未完成查询及控制上下文，自动保存JSON到AppLocalDataLocation/diagnostics/485，文件名含时间和唯一编号；连接页提供打开记录文件夹。重新连接清除当前会话收发缓存，但不覆盖已保存文件和最近异常路径。测试内存串口默认不写磁盘，可用QITEST_DIAGNOSTICS_DIR指定临时目录。异常自动保存失败明确显示；断开后手动导出仍可用。

`StartupSequence.h` 已实现用户确认的阶段规则：隔膜泵与TD加热属于同一启动阶段；两项确认后，新的有效真空读数严格小于8 mbar（8E0，2026-09-18用户最新确认，替代此前正E04门槛）才能请求分子泵开启。分子泵经设备回复确认后，按有效EFC流速判断载气状态：大于0采用小于0.01 mbar（E-03及更低），等于0采用小于0.0001 mbar（E-05及更低）；达到对应真空条件才请求离子阱升温。内、外载气仅为模式，不能代替流速；失效/未知流速不视为关闭。完成后仍随有效流速和真空回读更新真空就绪状态，不重复发送加热，也不擅自执行停机。阶段失败清空后续请求，不猜测自动关机顺序。

`PumpProtocol::powerCommand` 保存用户提供的启停ASCII原文（仅CR结尾），独立于主控板帧格式。按用户最新确认，不等待专用启停ACK：发送后重新查询310，新的有效电流大于0确认开启、等于0确认关闭。旧缓存、回显、其他参数、无效/迟到回复均不能确认。沿用旧COM4记录已验证的单COM接线，主控与泵各自编码并串行收发；未假设或新增第二个COM口。普通控制最多等待4.5秒；泵电流持续有效但未达目标的期限失败保留连接，其余通讯失败隔离串口，不自动重发启停。一键关机的转速归零等待不受这个总期限限制。

NetworkInstrument已连接开机策略、485队列和界面。设置与维护→常用部件的一键“开”，先确认外载气模式，再开启隔膜泵/TD初始阶段。一键开机目标温度固定为TD 250℃（允许范围220～280℃）、离子阱85℃（允许范围80～90℃），不取方法或参数预设。阶段完成表示加热设定已确认，不代表实测温度已经达标，也不改变样品检测要求离子阱实测温度严格大于85℃的门槛。等待真空不套用宿主5秒设置超时；“停止流程”仅停止后续步骤，不自动关闭已启动部件。一键关机已接入：先同时关闭TD/离子阱加热，离子阱低于75℃后停分子泵，转速和电流归零后停隔膜泵。RF按用户确认接485 RF_24V（0x10）；离子源高压按用户确认接485 HV_24V（0x09），夹管阀接网口放空（0x22）；已接入分子泵、隔膜泵、载气模式、温度和EFC流速。模拟和回环测试通过不代表实机验收。

2026-09-17用户修正EFC写入：485命令0x12的数据区为两字节大端整数，0～50 mL/min乘10（分辨率0.1 mL/min），覆盖此前文档的乘1000。方法下发及控制数据区编码已同步；状态回传仍沿用已确认的原值除200，此次未收到更改回读倍率的依据。

## 已内置网口TCP首版

2026-09-17气压连续曲线：0x82每周期的点按接收顺序追加，不再以最后一周期覆盖整次检测。X轴按厂家代码为累计点号/1000/60×（方法“周期”/data[8]×2）；用户确认前者来源于方法周期，后者为data[8]（当前映射cooling），两项都使用已成功下发的原值，在开始检测时冻结。默认10000/5000时点间隔4ms，显示时间/min。参数未知或data[8]为0时保留采样点轴，不猜时间。Y仍为大端uint16/65535×2.5×5.7 V。完成后保留整次检测，含关闭回执后连续的末帧；运行期不因5秒无气压帧清空已有点。下一次有效检测请求或断线清空；不新增历史归档气压回放。显示最多保留100万点，超限明确提示，不改动谱图采集判断。周期连续性检查保留，重复帧不重复绘图。

2026-09-15整周期帧修订（替代旧版分包组谱规则）：现场0x81长度3252含两字节周期编号，数据3250字节/1625点，一帧就是一周期。0x81/0x82长度后的两个字节按16位大端周期号解释，从0递增；状态及命令ACK仍用原1/1字段。仅波形帧放宽到16位长度字段可容纳的最大偶数数据区65532字节，TCP可多次读取后组成一帧。按用户随后确认的旧软件行为，上传波形（action=0x20、command=0x81/0x82）只记录CRC收到值与计算值，不因不匹配而拒收；状态、心跳和控制回执仍严格检查CRC16/MODBUS（FFFF初值、高字节先传），包括action=0x10的0x81方法回复。帧尾与长度检查保留。质谱点数须匹配当前方法，质谱/气压分别检查采集期周期连续性；质谱时间=周期编号（秒），强度原值和TIC/EIC求和保持不变。导出包含CRC策略及不匹配累计数，记录兼容接收不等同于证明原始数据无误。

在“仪器控制 → 运行状态 → 网口TCP”点击开始监听（默认11000），设备主动连接电脑。读取倍增管电压、真空规原始值、换算后的真空度（mbar）和实验状态；可与485同时连接，分别失效。0x82气压按用户代码换算为V，并按已确认方法的周期与data[8]计算分钟横轴，连续保留整次检测曲线；射频页支持调谐启停。调谐是现场常规操作，不因本地会话角色隐藏，但开始前仍需二次确认，且只有有效网口状态、实验停止时才发送。Fullscan 方法在有效状态且检测停止时，通过网口0x81下发并等待应答。已接入 Fullscan 定时采集：使用参数预设中的检测秒数，开启/关闭均等待网口应答，完整周期帧校验后保存原始强度、TIC/EIC序列；当前不执行实机谱库筛查。详见 [网口首版接入说明](../../docs/integration/网口首版接入说明.md)。

## 已内置485状态与方法基本设置

2026-09-16 COM4旧软件兼容：高压模块回读字段按完整uint16原值接收，移除原始值大于5000时拒绝整条状态的限制。新旧SPM记录均出现37887附近原值，旧软件持续查询；本次允许这类状态确认485连接并继续主控板/分子泵轮询。帧头、长度、帧尾、状态标志及电流/真空规/PWM检查保留。已有用户确认的原值÷10显示规则不变，抓包不代表物理量程已校准。Rs485Tests可通过QITEST_RS485_CAPTURE_DIR指定包含old-events.json和new-events.json的本地提取记录，按原始RX分块回放；客户记录不进入Git。

2026-09-16旧程序抓包兼容版：按用户要求，TCP action=0x10、command=0x30、payload改为0x23，每2000ms发送一次，取代旧网口表中的隔膜泵开关含义；485的0x30状态查询不变。连接建立后启用；接受方法请求时在第一条485设置前暂停，五项485确认和网口0x81等待期间不发送心跳，网口0x81成功后等待约100ms，按旧抓包顺序发送三条0x50/0x00，每条等待0x11确认；全部成功回调发布提示后重新计时。失败保持暂停，重试成功或新连接恢复；超时/断线停止旧连接定时器。发送日志与导出状态包含心跳，不把发送成功当作回读或方法确认。

在“仪器配置 → 运行状态”选择实际COM口后连接，9600/8N1/无流控；无需厂家插件。查询0x30状态，并可勾选同串口分子泵四项顺序查询；回读温度、流量、气压、原始真空规电压、高压模块读数和部件标志。设为当前Fullscan方法时，TD温度、离子阱温度、EFC、抽气PWM、进气PWM按队列逐项下发和确认。离子源电压未获写协议，非零值拒绝。错误/超时停止队列，读数超时仍按原规则失效。完整数据偏移、启动环境变量及实机验收项见 `../../docs/integration/VENDOR_INSTRUMENT_INTEGRATION.md`。

## 文件职责

解析诊断：TXT/JSON另存解析前RX_RAW原始接收块（最近最多128块/1MiB），采集首次错误冻结当时原始窗口及长度/帧尾/CRC等判定依据，后续状态和心跳不会覆盖错误现场。重新监听或下次开始采集会清除相应错误现场；波形CRC按上述兼容策略记录，其他解析检查保留。网口0x30的11/12回包单独计数，不再计为未解析帧。

- PumpProtocol.* / PumpReader.*：按用户四条ASCII查询和固定位置取数的分子泵只读试验；按用户确认倍率显示RPM/A/V/℃，保留原始值与有限收发日志，回复校验仍待确认。同一个COM统一排队查询，在同一485页面显示。详见[试读说明](../../docs/integration/分子泵试读说明.md)。

- NetworkProtocol.*：TCP帧、CRC与21字节状态解码（2026-09-09修订，实验状态01/00）。2026-09-14用户确认Fullscan：冷却时间→data[8]，进样时间→data[13]（输入380下发380，不乘100）；data[20]/data[21]为独立组合，本版按旧抓包改为固定1/3000，不随冷却时间改变；data[25—44]固定尾部亦按旧抓包，其他可编辑项及datafit校准换算保持动态。周期总时间使用data[8]。本次确认整数直传，非整数进样时间仍拒绝，不擅自截断或推测小数编码。
- NetworkInstrument.*：TCP监听、同IP新连接接替旧连接、读数有效期、连接事件/原始帧导出与485并行状态。支持TXT十六进制文本及JSON；TX表示提交发送，不代表设备接收/执行成功，未知方法返回码按原值保存；16页版协议的0x29按冷却时间错误立即结束本次方法请求，不再误报无回传。

- Rs485Protocol.*：最新485协议帧与23字节状态解码。
- Rs485Instrument.*：单串口统一调度主控板/泵；泵超时暂停泵查询，主控超时或串口失败关闭。
- IInstrumentAdapter.h：宿主与驱动的公共接口。
- IInstrumentPlugin.h：插件工厂与 ABI 标识。
- SimulatedInstrument.*：模拟实现，用来理解调用顺序，不要复制其假数值到真实驱动。
- VendorControlCatalog.h：协议控制数据区与出处，**不包含完整报文/传输/CRC**。

## 最短联调顺序

1. 从 ../../examples/vendor_adapter 开始。插件用与宿主一致的 Qt、编译器和位数。
2. descriptor 返回实际型号且 simulation=false。
3. health / telemetry / confirmedSettings 返回工作线程维护的实际回读缓存；不要阻塞 GUI。
4. validateSetting 检查能力、连接、单位、互锁及厂家安全范围。
5. requestSetting 保存请求号，异步发送；收到实际状态后发 settingFinished。
6. 方法参数走 validateMethodParameters / requestMethodParameters。Fullscan 的基本设置依次通过 485 命令 0x02、0x13、0x12、0x04、0x14 下发，全部应答成功后再通过网口 0x81 下发质谱设置；任一拒绝或超时立即停止。离子源电压在现有 485 文档中没有写命令，非零值必须拒绝，不能挪用网口 ESI 高压命令。
7. 控制器要求 requestId、key、readback 对应原请求；超时不自动重发。
   操作界面仅在真实适配器完成上述回读链后显示“设置成功”；预览路径不会显示硬件成功提示。
8. 最后才联调高压、泵等危险动作，台架与设备互锁必不可少。

## 建议断点与日志

main.cpp 的插件加载 → AppController::updateInstrumentSetting → 驱动 requestSetting → settingFinished 回调。
记录请求号、key、目标值、发送/回读时间、实际值和错误；不要只记录“发送成功”。
普通设置的宿主等待窗口为5秒；内置一键开机按上述阶段和读数驱动，各发送步骤仍有独立超时，真空等待可手动停止。不能把ACK当物理升温完成。
已接受但未完成的动作不能报 success=true 并伪造回读。

## 启用插件

在 PowerShell 设置绝对路径后启动宿主：

```powershell
$env:QITEST_INSTRUMENT_PLUGIN = "C:\Dev\driver\QITestVendorAdapter.dll"
& "C:\Dev\app\飞秒质谱工作站.exe"
```

未指定插件或485串口时使用轻量预览；指定插件加载失败会报错退出，不会更换设备来源。
尝试连接485或网口时，端口打开或TCP建立都不触发数据源切换；收到并校验有效状态帧后才切换。切换后若断线只显示断线，不自动回到预览。
连接页只保留一个“预览”入口。内部记录继续使用独立数据范围，预览记录不能混作真实采集记录。
详细契约见 ../../docs/integration/VENDOR_INSTRUMENT_INTEGRATION.md。
对应 tests/InstrumentControlTests.cpp；这组测试通过不代表实机通过。

插件 ABI 已升级为 1.2；接口新增方法参数校验、异步请求和完整回读，宿主与插件必须同步重编译。

2026-09-15补充：当前内置用户本次 datafit.json 的 Fullscan 系数（a=0.00013517703930585604，b=5.882440951161457，c=8.575019905095814），同步用于方法质量数转电压及扫描电压反算m/z。采集记录保存系数及来源文件SHA-256快照；历史记录不重算质量轴。按旧代码，上限超过801/1001分别需要datafit_1000/datafit_2000，当前未提供则拒绝该量程的方法下发。Iosolate分组未启用。

2026-09-16停止兼容：关闭0x15/0x23在500ms内未回复时再发送一次（最多两条），原3秒等待期限不延长；等待关闭时不再发送新增心跳。关闭仍须收到成功回复，无回执/拒绝/断线均不报完成。重发后收到成功，保留3秒迟到回复隔离窗口，期间禁止下一次开启，避免无请求号的关闭ACK被误认成开启ACK。500ms是对照旧抓包约533ms的测试实现，尚待实机验证。0x50后续已由2026-09-17抓包与用户确认接入方法离子源电压，见下文。

2026-09-17部件按钮补接：HV_24V开启/关闭为485 0x09的数据01/02，先等11应答，再查询30的HV状态位确认EE/FF；只连接485亦可操作。夹管阀命令为网口0x22、数据22/23，只连接网口亦可操作；成功11/失败12，应答3秒超时即隔离旧连接、不重发，迟到回复不确认。夹管阀没有独立位置回读，界面明确显示“开启/关闭已确认”（命令应答）；断线或状态失效后未知。以上未纳入一键启停，不改变既有开机顺序。

2026-09-17 RF补接：RF按钮控制485 RF_24V（0x10），开启01、关闭02；等待11成功应答后查询30，以数据区第5字节（从0计）的EE/FF确认实际开关状态。与HV_24V共用有界串口控制队列；只连接485亦可操作，不发送网口RF24V/48V命令，不加入一键开机步骤。

2026-09-17重启恢复：总运行状态根据新鲜网口和485回读、隔膜泵状态位与分子泵电流恢复；双泵均开启为运行中，均关闭为泵已关闭，不一致/缺失为未知。真空就绪独立按实际流量和压力判断；温度达标只表示实测范围，不据此推断加热器电源。已有分子泵电流时不重复切换载气或发送启动泵指令；未取得有效泵电流时等待回读。断线不沿用已开启缓存。一键关机按下述用户确认顺序执行。

2026-09-17升温诊断：开机步骤失败优先显示原失败原因，不再被“泵运行、真空就绪”覆盖；TD250和离子阱85的设定回执与实测温度达标分开显示。双泵已运行、温度低于目标范围且本连接尚未确认两项设定时，允许操作者明确点击“继续开机”，只补温度步骤：已有TD250确认则跳过TD，真空满足当前实际流量门槛才发离子阱85；不自动续发、不重复泵或载气命令。0x08关闭加热时清除温度设定确认。485导出busStatus.controlHistory保留最近96条部件控制发送尝试、匹配回执及结束/失败记录，独立于256条轮询报文缓冲；断线保留证据，重新连接清空。未确认实机不升温的具体原因，不能把ACK当成加热器实际工作证据。

2026-09-17关机顺序：ShutdownSequence通过485 0x08/02同时停止TD与离子阱加热，等11应答和30加热状态位FF确认后，持续查询离子阱温度。严格低于75.0℃才发分子泵关闭原文；关闭后清空旧泵读数，持续轮询主控和分子泵，以指令后的新电流0及转速0 RPM确认，再发485 0x11/02关隔膜泵，11应答和状态位FF后完成。TD降温不阻塞停泵。物理降温/减速不设全程5秒超时，单次回读仍限时；拒绝、超时、断线或取消停止后续步骤，不报成功、不重发停泵、不自动回滚。重启识别设备运行后可点击关；RF/HV、载气和夹管阀不在用户指定的关机动作中。

2026-09-17常用部件就绪状态：operatingConditionsReady使用当前有效的真空就绪状态及实测TD 220～280℃、离子阱80～90℃，与上方开机达标提示共用判定；不依赖方法下发。串口或网口失效、任一项不达标即为false。health.ready及检测方法回执、真空、严格大于85℃的采集前检查不变。

2026-09-17离子源电压方法下发：old.pcapng中方法成功后0x50连续三条BE，用户确认设定3800 V；结合协议2.5V控制量对应5000V、控制量乘100，采用V/20编码为单字节（0～5000 V，20 V步进）。3500 V为AF，3800 V为BE，0仍发00。预检在任何写入之前完成；485只处理TD/离子阱/EFC/PWM，source由TCP接入，三次0x50均成功后才确认整套方法并恢复心跳。TD=0按用户确认仍发485 0x02数据0000，不跳过、不替换关加热命令。监控温度/电压保持设备回读，不能用设定值覆盖。此次旧抓包有气压波峰，新旧载气模式和多项方法参数不同，不能把0x50修复视为气压问题已实机解决。

2026-09-18手填质量轴校准：NetworkInstrument持有本实例Fullscan配置，默认沿用原datafit系数；NetworkProtocol接受显式配置，两向计算共用。同步配置时清空当前连接的方法确认，忙于方法/采集/调谐/控制时拒绝切换；必须重新发送0x81及全部后续成功回执才可检测。诊断massCalibrationProfile保存当前配置；采集开始冻结质量轴，不修改旧谱。配置损坏阻止下发，不静默回退默认。未添加或猜测独立“校准同步”下位机命令。
