# 单机操作与安全

当前交付不显示登录页，也不按本地账户角色限制功能入口。AuthorizationPolicy
只保留兼容接口，所有工作站会话均可使用完整功能。

这不取消硬件安全边界。新增硬件动作必须同时核对真实连接、参数范围、用户确认、
设备自身互锁和实际回执；不能只靠界面按钮状态判断设备已经执行。
模型、插件和导入文件都不能绕过这里。对应 tests/SecurityTests.cpp 与 InstrumentControlTests.cpp。
