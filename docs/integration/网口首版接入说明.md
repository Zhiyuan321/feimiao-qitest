# 网口首版接入与联调

依据：用户提供2026-09-09提供的《质谱网口通讯协议(2).pdf》，共15页。当前是内置TCP状态回读、单包气压波形及受控调谐启停接入，可与原有485同时工作，不需要厂家插件。未完成实机和Windows 7目标系统验收。

## 在界面连接

1. 进入工作站 → 设置 → 仪器控制（代码模块名“仪器配置”）→ 运行状态 → **网口TCP**。
2. 本机IP默认 `0.0.0.0`，表示监听所有IPv4网卡；也可在下拉列表选择连接仪器的实际网卡IP。默认端口 `11000`，点击“开始监听”。如启动后更换网卡，可直接输入新的本机IP。
3. 仪器作为TCP客户端，目标地址填写电脑实际网卡IP和11000端口。**不能把0.0.0.0作为仪器目标IP**。电脑与仪器需网络可达；如Windows防火墙提示，允许此程序在仪器所在网络通信。软件不修改防火墙规则或网卡配置。
4. “正在监听”表示服务器已启动；“TCP已连接”只表示套接字已连接；收到CRC和内容均有效的状态帧才显示“网口状态回读正常”。
5. 观察倍增管高压、真空规原始值、真空度（mbar）、实验状态。真空度按2026-09-09用户确认的公式与mbar单位换算，同步显示在主界面；实验开启也不代表工作站已接入真实谱图采集。
6. “状态超时”默认5秒，可在开始监听前改为1～3600秒。这是上位机保守失效策略，协议没有规定上传周期，应按设备实际周期调整。收到其他命令的帧不能刷新状态有效期。
7. 状态超时后清空网口测量值，保持TCP等待新的状态；TCP断开后清空读数并继续监听，设备重连后必须重新收到有效状态。停止网口监听不会关闭485；断开485也不会关闭网口。
8. “导出最近报文”保存JSON：最近256条CRC有效接收帧/调谐发送帧、时间、对端IP、动作、命令、分帧字段、完整十六进制报文和计数。另附最近64条连接事件及acceptedConnections/replacedConnections/rejectedConnections计数，帮助定位旧连接未关闭时的重连。包含未解析谱图帧，但不组谱、不持久保存所有采样点。不是完整采集记录；错误CRC/帧尾/长度的字节只计入丢弃计数。

默认启动保留轻量预览。开始监听、TCP连接或打开485端口都不立即更换数据源；只有收到并校验有效状态帧后才切换。切换后若断线只显示断线，不自动恢复预览。连接页可手动选择“预览”。已加载厂家插件时，内置网口/485入口不能替换插件。保存IP/端口不代表下次启动自动监听。

## 首版明确采用的协议规则

- TCP服务器角色依据第6页的QTcpServer配置，上位机监听11000；同一时刻只读取一个客户端。同一仪器IP的新连接接替旧连接（源端口可变），先使旧读数失效、丢弃旧连接半帧，再等待新连接的有效状态；其他IP的新连接在当前设备仍连接时被拒绝。IP相同不是设备身份认证，仅用于当前直连仪器的重连策略。
- 帧：`55 ACTION CMD LEN_H LEN_L COUNT INDEX PAYLOAD CRC_H CRC_L AA`。
- 第4页长度定义优先：`LEN = PAYLOAD字节数 + 2`，整帧长度 `LEN + 8`。解析器允许数据区1～1024字节；后面单个命令最大1000字节的表不会扩大状态长度。
- CRC按第5页算法：初值FFFF、反射多项式A001、无最终异或；计算范围ACTION至PAYLOAD，**CRC高字节先传**。不能套用普通Modbus线路的低字节先传规则。
- 数据多字节按大端。状态只接收 `ACTION=20、CMD=01、COUNT=1、INDEX=1、LEN=0017`，数据区恰好21字节。单帧序号1参照协议示例；其他编号保留为未解析帧，不猜布局。

| 数据区偏移（从0起） | 映射 |
| --- | --- |
| 0、1 | 大端无符号16位倍增管电压，V，范围0～3000；以第13页两字节示例为准，未采用该行U8笔误 |
| 2、3 | 大端无符号16位真空规原始值；按下述公式换算为mbar |
| 4～8 | 文档标“无用”，保留位置、忽略值，不覆盖485电源或泵状态 |
| 9 | 01=实验开启，00=实验关闭；其他值拒绝作为状态读数 |
| 10～20 | 占位，保留位置、忽略值；末尾11不能用来判断实验状态 |

第13页长度为21+2；第14页“数据区长度为23”是混用了长度字段与数据区长度。按字段表与实机报文：数据区21字节，长度字段23（0017），整帧31字节。旧14字节及旧11/12状态值不属于当前布局，不自动猜测兼容。

本地测试黄金帧（合成数据，不是实机抓包）：

`55 20 01 00 17 01 01 0B B8 04 D2 00 00 00 00 00 01 00 00 00 00 00 00 00 00 00 00 00 4C B2 AA`

期望：倍增管3000 V、真空规原始1234、实验开启。单独收到TCP连接、错误CRC、未知命令、错误数据长度或状态枚举都不构成有效状态。

## 真空度换算

2026-09-09用户补充公式，并确认pressure单位为mbar：

```cpp
result = (value * 1.0 / 65536) * 2.5 * 5.7;
pressure = pow(10, (result - 6.143) / 1.286);
```

value为网口状态偏移2、3解析后的无符号16位原始值。仅有效且未超时的状态参与换算；断线、停止、超时和连接接替时清空真空度，等待新状态。原始值0按公式计算，不将缺失数据当作0；不额外猜测故障哨兵或量程。485真空规字段是mV，不套用此ADC公式。计算保持double精度，界面使用科学计数法，JSON状态附vacuumMbar。

核对值：原始2826 → 5.02336351856E-05 mbar；原始2822～2831 → 5.01554677013E-05～5.03315158786E-05 mbar。换算的软件测试不能替代真空规实物校准。

## 2026-09-15 确认的检测与 TIC 接入规则

- 用户确认：0x81 上传用于质谱图；每个完整周期为 1 秒，TIC 横坐标单位秒，每周期产生一个点，纵坐标为该周期完整质谱所有强度之和。不按 m/z 做梯形积分，也不将此前周期的强度累计进当前点。此前确认的 0x82 仍为气压数据通道。
- 通用 ChromatogramEngine 已实现逐谱求和，时间来自 SpectrumScan::timeSeconds；该确认针对实机接入，不能覆盖导入记录自带的时间。网络接收回调、协议分包与完整扫描是不同层级，不能每收到一个分包就生成一个 TIC 点。
- 检测控制协议：PC→单片机命令 0x15，数据 0x22 开始、0x23 停止；单片机→PC 同命令返回 0x11 成功、0x12 失败。依据桌面《质谱网口通讯协议.pdf》第 7、8 页；截图中的反馈表不能代替下发数据表。开始应答成功不等于检测完成。
- 用户后续确认：0x81 一个采样数值占 2 字节，按协议大端顺序组合；质谱纵轴直接使用上传数值，不加倍率、归一化或平滑。不要套用 0x82 的电压换算。TIC 对每张完整质谱的这些数值求和。
- 质量轴算法已在用户提供的《全扫方法参数.txt》末尾 protocol->x[i] 循环中找到：由 sum_count 和实际下发的 low_v/high_v 得到各点扫描电压，再使用 calibrate_a/b/c 反算 m/z。系数来自对应量程 mapper/datafit*.json，应沿用实际仪器校准文件，不能以质量范围简单线性插值代替该算法。
- 总包数和当前包序号的含义已由协议第 4 页明确，可以据此检查并拼接完整数据组；已实现连续序号、总包数、每包偶数字节及整谱点数检查后组谱。
- 仍待明确整次检测完成的通知：用户表示完成时应有结束指令，但未提供具体命令码和数据区。已核对本次重新提供的 16 页《质谱网口通讯协议.pdf》：0x15/0x23 是 PC 下发停止，0x15/0x11 是控制成功回复，0x72 是 SWIFT 发送结束；这些均不能擅自当作下位机的整次检测完成通知。实验状态 01/00 已有定义，但它与正常完成、主动停止及末包的关系未明确。
- 已按用户指定的上位机定时方案接入：读取“参数预设/检测时间（秒）”，发送 ACTION=0x10 CMD=0x15 DATA=0x22；收到成功 ACK 后开始计时，到时发送 DATA=0x23。关闭 ACK 成功且至少一张完整谱、无残留分包/校验错误才进入存储；缺包、断线、拒绝、3秒ACK超时均不弹成功提示。取消也须等待关闭确认；窗口关闭期间先请求取消并等待。
- 当前仅支持周期10000（1秒）的 Fullscan，最多5000周期、100万采样点。质谱轴反算使用与当前下发方法一致的已提供系数，点数按旧代码 rf_frequency * data[9] / 10 计算；2026-09-15用户已提供 datafit.json，当前方法与反算统一采用其 Fullscan 三系数；超过801的范围需另提供 datafit_1000.json，超过1001需datafit_2000.json，缺失则阻止方法下发。历史谱图保持保存时质量轴。
- 每谱首点时间从0秒开始，按完整谱序号每次增加1秒。协议只有组内分包序号，没有整次检测的周期唯一编号，因此不能识别设备完全未发送的一整周期，也不以墙钟强行补点。关闭前传输的末包顺序需实机验收。
- 当前保存真实原始强度、扫描序列和检测/预设快照，TIC按每谱求和，EIC按质量窗口求和。来源标为“实机采集 · 筛查未配置”；不会对实机数据运行内置演示谱库，也不据此生成阴性/阳性结论。
- 本地回环已验证分包/粘包、U16高位值、TIC求和、计时关闭、缺包/CRC/ACK超时、保存及重开。实机采集与最终Win7环境尚待验收。
- 其余新增预设仍只保存：校正数值、强度阈值、背景扣除/噪声/信噪比的算法，旧谱库格式及匹配规则，定量方法与校准文件、维护方法与气体辅助的具体动作/时序，需要旧软件实现或厂家说明后逐项接入。原有硬件参数仍通过方法设置下发。

## 尚未实现／后续修改位置

| 待确认功能 | 原因与位置 |
| --- | --- |
| 通讯测试动作50 | 第7页长度0001与第4页定义不一致；首版不主动发送测试命令 |
| ESI高压设置 | 第8页1字节、量程与倍率冲突 |
| 完整方法下发 | 第9～12页部分字段单位、比例、有无符号缺失 |
| SWIFT下发 | 第9页帧数只写/500，数据编码、帧数和结束语义需确认 |
| 周期气压82 | 已按用户代码实现U16大端与V换算；完整周期时间轴/重置规则仍未定 |
| 高压/泵/电源等控制 | 除下述调谐启停外不发送TCP写指令；需要确认通道及实际回读契约后开放 |

`src/device/NetworkProtocol.*`负责帧/CRC/状态字段，`NetworkInstrument.*`负责TCP与485共同状态，`src/ui/NetworkConnectionPanel.*`负责连接面板。新规则应加在相应层，并更新回归测试和本文。Qt Network已是原工程依赖，不增加第三方库、不修改插件ABI。

## 本机回归与实机验收

构建 `QITestWorkstation、qitest_network_tests、qitest_rs485_tests、qitest_device_tests、qitest_ui_tests`。执行：

```text
ctest --test-dir build/win7-qt51212-release -R "qitest_(network|rs485|device)_tests" --output-on-failure
qitest_ui_tests.exe -platform offscreen networkPanelConnectsAlongside485 rs485StatusPanelReadsAndInvalidates instrumentPowerButtonsReflectPartialState
```

2026-09-09实机导出的4帧用于本地回放：应解析出倍增管0 V、真空规原始值2827/2823/2828/2826、实验关闭。设置环境变量QITEST_NETWORK_REPLAY_JSON为原JSON的本地路径可执行capturedStatusReplay；原始JSON、设备地址和时间戳不入库。回放通过不能代替新软件在Win7设备上验收。

网络测试覆盖CRC固定向量、全切分点、粘包、噪声、CRC/帧尾/长度错误、保留字节、错误状态、TCP等待/断线/重连、读数超时、双通道独立失效、同IP新连接接替、不同IP拒绝、连接事件有界、原始帧缓存上限及导出。界面测试使用本机回环连接与假串口，无实机。

实机需验证：设备是否主动连接11000、是否自动上传上述21字节状态、CRC及长度是否与文档一致、读数是否随设备变化、实际上传周期、拔网线后的失效/重连；同时开启485检查温度/流量不受网口断开影响。2026-09-09独立接收工具日志显示：收到3帧后，同一仪器IP更换源端口重连1231次，原“已有连接即拒绝”策略无法恢复。现已改为同IP接替旧连接；仍需在现场验证是否恢复持续读数，以及固件为何频繁重连。遇到不匹配时导出报文，保留设备固件版本和现象后再修改协议，不用猜测值替代真实读数。

### 2026-09-15 实际 datafit.json 核对

Fullscan：a=0.00013517703930585604，b=5.882440951161457，c=8.575019905095814。来源文件SHA-256：f84190db1ebd30dc63eafcc8f69c726f3cefa73b65f07130378e79229b8e2233。与上一版默认系数不同，已替换两向计算的公共系数。当前为构建时内置该配置，非启动时重新读取任意同名文件。

独立核对：质量数30/40/300按多项式除2并截断后，下发槽5/6/7分别92/122/892；默认1625点的反算轴首尾为39.98490795943708、299.7529579296157。边界偏差来自旧代码规定的整数截断，不强行改成40/300。强度和TIC公式未变。采集快照包含系数/来源摘要，便于重开核对。AC线性校准仍沿用先前配置，此文件不包含AC参数。

### 方法确认丢失后的恢复

用户视频显示方法设置成功后开始检测仍被阻断，视频不能判定是否发生重连。代码可复现网口断开清除确认而本机方法仍活动的情形。界面已允许同一活动版本重新下发；不沿用旧连接ACK，也不自动发送采集命令。启动拦截区分缺少方法/缺少参数/当前连接未确认/参数不一致；网口JSON诊断附methodConfirmed、methodConfirmationReason、confirmedMethodParameters，工作库审计保存拦截时两份参数快照。对应测试验证断开重连→保持本机活动→阻止启动→同一方法重新ACK→采集成功。
# 2026-09-15：网口心跳补充

用户确认网口命令0x30改为心跳，数据区仍为单字节0x22，action为0x10，count/index均为1，长度为3；CRC和帧头尾沿用已有网口规则。只改变网口0x30，485状态查询不变。

- TCP建立后每2秒发送；不以状态回读或收到心跳回复作为启动定时器的前提。没有新增心跳应答超时断线逻辑。
- 方法请求校验通过后、第一条485设置之前停止心跳，随后原顺序下发五项485基本设置和网口0x81。仅在0x81已发送并等待应答时接受方法确认。
- 收到0x81成功应答后，先走原方法确认/界面提示，再重启2秒计时；发送方法、心跳回复、未知方法回复均不触发恢复。
- 方法拒绝保持暂停；允许再次设置并在成功后恢复。超时和断线停止定时器，新连接重新启动。不会发送0x23来“暂停心跳”。
- 心跳TX进入同一个有界十六进制日志，导出状态增加heartbeatIntervalMs、heartbeatActive、heartbeatPausedForMethod和heartbeatSent。它们反映上位机调度，不代表下位机已确认。

验收：本机TCP回环与假485覆盖2秒间隔、跨485/网口等待暂停、成功提示顺序、失败/超时/重连及导出；实际是否消除现场5秒重连仍须使用新包验证。

### 2026-09-15：采集解析失败现场保留

现场08:28:53Z导出记录：08:25:38Z起同一连接保持到导出，心跳0x30收到0x11。08:27:38.478Z发开启、.507Z收成功，08:27:39.031Z因解析失败发关闭、.033Z收成功。累计rejectedBytes=7536，acquiredScans=0，旧导出不含被拒原文，不能仅据此判断是CRC、长度定义或数据损坏。

新导出保留解析前原始接收块rawReceives（最多128块且不超过1MiB），采集首次失败时冻结相同窗口到acquisitionErrorRawReceives。记录时间、来源端口、监听期间累计字节偏移及原文，TCP读取边界不等同协议帧边界。错误信息acquisitionParseFailure包含首次被拒候选的原因、声明长度、候选原文，以及适用时的接收/计算CRC。首次错误现场不因后续空闲流量或断线消失，在下次开始采集或重新监听时清除。原有CRC有效帧导出仍保留；不改变CRC/帧长度或丢包判定，不把不完整数据作为成功结果。

0x30的0x11/0x12回包单独标记heartbeatReply并累计heartbeatReplies；不刷新仪器状态时限、不完成方法设置、不认作采集数据。

### 2026-09-15：完整周期、周期编号与强制CRC（历史阶段，CRC策略由下节替代）

用户在原始报文核对后明确：CRC必须按文档查表算法校验，周期编号从0开始逐周期加1。实测第一张0x81头为55 20 81 0C B4 00 00，下一张为55 20 81 0C B4 00 01；每帧总长3260，数据区3250字节/1625点。0x82长度500，总长508，数据区498字节/249点。此实现替换上文历史分包组谱逻辑：两字节周期号大端解释，TCP读取的1460/1460/848等边界不作为协议分包。

只有action20的81/82可接收超过旧1024字节上限的波形数据，最大数据区65532字节；其他帧保留旧长度限制。全部保留帧尾AA和CRC强制校验，CRC计算从动作到数据末尾，FFFF初值/A001多项式、高字节在前。CRC不匹配时显示命令、收到值、计算值并保留原文，不因CRC字段为零而绕过。实际四帧CRC字段0000，与计算值0F8E/13B8/E283/7128不符；这一事实仍需下位机侧检查，软件兼容大帧不代表这些原包变为有效。

完整质谱通过CRC、当前方法点数及周期连续性校验后才发布；每次检测首周期必须为0，跳号/重复均失败，分别检查气压和质谱周期。质谱时间取周期编号，1周期=1秒，TIC为该张原始谱强度之和，EIC沿用窗口内求和。不会把一条大帧拆为多个TIC点。气压显示最近完整周期的采样电压，不虚构周期内采样时间。停止仍须设备确认，存在未收齐报文或没有完整谱时不报成功。

验证包括现场原帧回放（仍因CRC失败）、仅在内存中替换CRC的合成对照（可解析）、完整大帧拆/粘包、0/1和255→256周期、重复/跳号、错误点数、坏CRC、TIC/EIC以及保存再打开。原始客户报文不修改、不提交Git，不将合成对照称为实机通过。

### 2026-09-15：按旧软件行为兼容波形CRC（当前策略）

用户提供旧软件接收函数，并确认CRC计算后没有比较校验；随后授权修改。仅上传波形action=0x20、command=0x81/0x82采用record_only：保留帧中CRC两个字节，计算并记录差异，但不因CRC不匹配而丢弃波形。不是仅放行0000，也不修改接收原文。其余状态、心跳及控制回执仍强制CRC校验，尤其action=0x10、command=0x81的方法回复不在兼容范围内。

长度、帧尾AA、质谱点数、周期连续性、未收齐报文和停止确认仍沿用上述检查。TXT/JSON导出逐帧记录crcRequired、crcMatches、receivedCrc、calculatedCrc；状态记录waveformCrcPolicy和waveformCrcMismatches，检测归档sampleInfo保存waveform_crc_policy=record_only。CRC不再拦截意味着无法通过CRC发现波形内容损坏，不将兼容接收描述为数据已通过CRC验证。

验证：原始四帧不改任何字节，经TCP读取边界拆分后全部解析，保留0000及其计算值；合成测试覆盖非零CRC不匹配、坏控制CRC仍拒收、坏帧尾/错误点数/跳号拒收、TIC/EIC及采集保存再打开。客户报文只在本地build目录回放，不提交Git。本机验证不能替代新包实机验收。
