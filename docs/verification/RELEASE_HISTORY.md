# 2026-09-03 当前交付收尾

用户确认当前布局可用，后续仅做小幅稳定性修补与示例入口，不再扩展功能。

## 本次范围

- 保留 Qt Widgets/C++17；Windows 为 Qt5.12.12/GCC7.3 x64，Mac 为 Qt6.10.2 arm64。Mac 并不是 Qt5.12 构建。
- 谱库、校准等浅色页面已修复；旧进程不会自动加载新构建，已正常重开工作站确认，未关闭厂家 Wine 程序。
- PDF 改为自动换行/分页、原子写入、每次独立文件名；非法候选选择不覆盖已有文件。长名称/长证据按实际输出检查，不把界面正常当成 PDF 完整性证明。
- 内置 OpenMS BSA 的 64 次扫描片段（约 1.7MB CSV，压缩编入资源），TIC 空状态旁提供“载入示例曲线”。明确操作后后台导入独立 PUBLIC_EXAMPLE 记录，再显示 TIC/MS/EIC；不会启动 AI、发送仪器指令或覆盖原记录。重复载入复用同一记录。
- 示例 EIC 默认 391.284103 ± 0.5 Da；用户可点选其他谱峰。此数据不是已知浓度标准，不作检出限或定量准确度证据。来源及许可见 tests/fixtures/public_ms/README.md。
- 深度问答开关只显示当前状态“已开启 / 已关闭”，不再把状态和反向操作拼在同一标签中；按需加载逻辑不变。
- 保留已实现的手动积分、内/外标线性校准、私有标准比对与独立证据导出。本次不加入尚未完成的比对证据到检测记录的新表/新接口，避免交付半成品；现有功能没有删除。

## 包的用途

- Windows7 可运行 ZIP：完整解压后双击 EXE，包含 Qt、CPU AI 运行时和模型，不需要编译。
- Mac ZIP：解压后使用 .app，包含模型；仅本地临时签名，不是公证发行。
- 轻量源码 ZIP：不含模型权重，用于修改/重新编译。
- 完整源码与模型 ZIP：相同源码加 Qwen GGUF，用于离线留存；它本身不是已编译应用，仍需工具链。

未接通硬件、完整厂家鉴定对齐、真实 Win7 工控机/老 CPU 耐久验收的边界不变。具体未实现功能见 VENDOR_INTERFACE_OPEN_ITEMS.md，不因本次交付而宣称全部完成。

## 收尾验证

- 19:50 后的小修：将调谐与校准、进样与注射泵、清洗液抽取三个未接入说明页合并至“仪器配置 → 硬件接入说明”，分别保留缺失条件，不增加硬件操作。旧助手导航仍指向同一说明页。视图说明更新为 TIC/质谱/EIC；独立计算功能及设备接口未删除。

- 原生 Mac 与 Qt5.12.12 Windows 测试程序（Wine、offscreen）均通过 `bundledExampleLoadsThreePlotsWithoutAi`：64 次扫描、三张曲线非空、重复载入复用记录、不自动启用 AI、深度问答两种状态文案正确。
- 报告测试覆盖正常导出、长内容分页和非法选择不覆盖文件；Mac 5 页及 Windows 4 页长报告已检查，分页差异来自平台字体。
- 上述为局部回归，不等于真机 Win7、真实仪器通讯或长时间大数据耐久验证。模型加载速度和峰值内存仍取决于客户 CPU、内存及输入规模。
