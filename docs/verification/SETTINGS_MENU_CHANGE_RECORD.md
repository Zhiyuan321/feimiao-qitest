# 设置与交互更新验收 · 2026-09-03

## 修复

- 设置的仪器配置折叠层改为固定标题和直接入口，切换模块不再收起其他项目。
- 顶部设置再次点击和侧栏 × 均收起设置，返回之前的工作页面；助手“打开设置”仍保持只打开语义。
- 智能台显示模型加载、生成回答及经过秒数；正在回答时提供停止入口。重复发送不会堆积 UI 请求。
- 同步此前 Mac 的模拟启停联动、图标、实际检测计时及 800×480 紧凑布局等补丁。

## 本轮证据

- Mac Release 构建和 8 组 CTest 通过；最后设置开关补丁后完整 UI 组再次通过，包含重复点击、返回原页面、弹窗关闭和 80 次导航控件数量检查。
- Windows 使用 Qt 5.12.12 x64、WINVER=0x0601 的既有 MinGW 工具链构建；8 组 Windows 测试在 Wine 11.0 / Win7 前缀下全部通过。
- 首次测试误用了 Wine 应用启动包装器，未产生新测试日志并超时；更正为实际 wine/bin/wine 后全套通过。没有把旧日志作为本轮测试结果。
- Qt5 依赖闭包、EXE 子系统版本、已知较新 API 静态导入检查及发布目录 SHA256 均通过。新 ZIP 内每个文件及模型经过 SHA256 和 CRC 校验，并解压至独立验收目录。
- 解压副本的 Windows 主进程实际启动并持续运行；Wine UI 辅助功能抓取超时，不能算完成可视化人工验收。界面布局证据来自 Windows Qt 测试截图。
- 最终 Mac 应用签名、arm64、Info.plist 检查通过；实际点击确认设置可以再次收起。真实本地 Qwen 试问得到中文回答并恢复发送按钮，随后关闭深度问答释放自有模型进程。这不是精确速度基准，也不保证旧电脑十秒内回答。

## 交付与边界

新 Windows ZIP 名称：`飞秒质谱工作站-Windows7-x64-Qt5.12-8GB-更新版20260903.zip`，3061606374 字节，包含模型。

旧已发送 ZIP 保持 3061581352 字节、修改时间 1788436577，不覆盖。Mac 仅保留当前交付目录的完整 App，删除可由工作源码重建的开发预览 App。

没有真实 Windows 7 工控机、触摸屏或仪器。测试通过不等于没有全部 bug，不代表真实硬件接口已接通；当前默认仍是模拟适配器。
