# 公开扫描数据回归检查（2026-09-03）

## 不变的技术边界

C++ / Qt Widgets，Windows 目标继续使用 Qt 5.12.12 x64，不迁移到 QML、WebView 或网页框架。当前 Mac 开发构建使用已安装的 Qt 6.10.2；不能将该构建结果冒充 Qt 5.12 验证。Windows Qt 5.12 交叉构建和 Wine 测试另行执行并记录。

## 本次完成

- OpenMS 固定版本公开 BSA 原文件已下载至 `.qa/public-ms/BSA1.mzML`，13.0 MiB。来源/哈希、许可、转换方法见 `tests/fixtures/public_ms/使用说明.md`。
- 小样本为 64 扫描 / 32,248 点 / 1,736,671 字节 CSV。编入测试程序，未写入客户工作区或业务谱库。
- C++ TIC/BPC/EIC 对独立 Python 参考逐点验证，相对误差阈值 1e-10；积分验证无基线与有符号端点直线基线两种定义。
- CSV → SQLite → QITest 归档 → 重新读取后，时间轴与 TIC 无变化。
- 修复积分输入框时间边界舍入问题，以及分析弹窗透明底色、下拉框与主操作层级；曲线绘制限制在绘图区，不越界涂到坐标文字。
- 校准输入保留空列并严格检查两列，不把缺失响应移位误读；异常保留之前的校准数据。

## 已执行的检查

- Mac 全部 8 个测试组通过（23.65 秒）；随后坐标边距修正再通过全部 UI 测试。包括公开数据回归、存储、UI、AI 生命周期和安全检查。
- Windows x64 Qt 5.12.12 交叉编译通过。
- Wine 中 Windows `core`、`workspace`、`ui` 通过；主题修正后再次执行 `ui` 通过。日志：`.qa/qt512-tests/core.txt`、`workspace.txt`、`ui.txt`。
- Mac 截图：`.qa/professional-analysis/`。这是开发构建检查，不是原有交付 ZIP 的验收。

## 不能据此声称的事项

公开文件头 TIC 和保存谱点重算 TIC 不同，来源定义不能混用。本数据没有浓度真值，不验证定量准确度。合成校准测试验证数学行为，不证明仪器方法适用性。内标批量配对、私有谱库编辑、批量积分等未完事项继续记录在 `厂家接口待补项.md`，不能因出现按钮而标完成。

## 重复图修复与绘图检查

- 单谱记录只显示一个 m/z 图，不显示虚假的 MS/MS 或空 TIC 卡片；有至少两个 MS1 时间点时展示 TIC + 当前扫描 + 筛查结果。
- 公开 64 扫描测试通过真实导入与数据库恢复进入运行页，点击最后一个 TIC 时间点核对对应谱点。1024×680、1366×768 和侧栏组合验证完整边界。
- `.qa/professional-analysis/tic-ms-linked.png` 为实际测试窗口截图，不是设计稿；百万级信号坐标预留字体宽度，历史记录哈希移入悬停说明。
- 10 万点测试验证绘图缓存按像素保留端点及极值、连续悬停不重复建缓存、缩放重建、后台 CSV 仍导出全量 10 万点。绘图降采样不改变算法输入。
- 本次新 Mac 开发应用已打开；原交付路径仍为旧包，两者不可混淆。桌面自动化在原生导入窗口发生超时，现场导入检查尚未确认。

没有进行真实 Windows 7 工控机、仪器 SDK、连续半年运行验收。本轮尚未重新生成交付 ZIP，旧 ZIP 不含本轮全部修改。
