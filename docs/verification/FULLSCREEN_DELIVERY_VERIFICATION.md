# 全屏与 4:3 交付核验

阶段记录：此后 Mac 已改为默认窗口，模型已改为 0.5B；当前配置与实际交付请参阅 ../LIGHTWEIGHT_ASSISTANT_AND_FIXED_MENU.md。下列 EXE 哈希及“模型未替换”仅对应上一轮全屏版本。

- Mac 原生构建通过；完整 UI 测试 14 项通过，最后控制按钮扩展后布局/启停针对性测试 4 项通过。
- Mac 交付 App 已更新及重新签名，签名校验通过。Cocoa 隔离数据库启动检查通过；桌面截图工具此次仅返回黑屏，未把它当作可见全屏验收证据。1024×768 控件渲染来自原生 UI 测试截图。
- Windows 使用 Qt 5.12.12 / GCC 7.3 x64 构建；最终 UI 测试在 Wine 中 14 项通过，设备模型/控制测试 6 项通过。窗口模式测试容许系统标题栏/任务栏占用高度；全屏切换单独验证。
- Windows EXE SHA256：8fd9d54226f5dbfe0486bd323756c673b132f76802e8b53bc41a10f55f1464c4。
- 最终 Windows.zip 已逐文件校验 SHA256 和 ZIP CRC，包括模型。模型与用户数据没有替换；源码包不含模型权重。
- 当前低配 Windows 7 实机、1024×768 实际触摸屏和仪器联调尚未验收，不承诺与 Mac 相同帧率/推理延迟，不宣称厂家协议已完整接通。

完整草案缺口见 ../FIXED_LANDSCAPE_AND_DRAFT_REVIEW.md。固定主页面不等于所有长表格、对话与编辑弹窗完全取消内部滚动。
