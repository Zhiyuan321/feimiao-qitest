# 飞秒质谱仪项目工作约定

- 本仓库只维护飞秒质谱工作站，不混入 SCIENTZ 或其他项目的源码、模型、数据库和发布物。
- 开始修改前先读 `先看这里-中文导航.md`、相关模块的 `使用说明.md` 以及受影响测试。
- 只检查与本次任务直接相关的入口、调用关系、状态、数据契约和测试；不要主动进行无关重构。
- 修改完成后只运行相关测试；相关测试失败且排查需要时才扩大范围。
- 保持 Qt/C++ Widgets 原生实现。Windows 当前构建基线为 Qt 5.15.2 / MinGW 8.1 x64（保留 Qt 5.12.12 旧构建入口）、Windows 7 SP1、8 GB 内存及 1024×768 / 4:3 屏幕；macOS 独立构建。
- 主流程固定为“方法选择 → 样品分析 → 报告查看”，不得恢复已经删除的“检测记录”。
- 科学数值由确定性 C++ 引擎计算；AI 只做本地检索、解释和受控操作建议，AI 不可用时核心流程仍须可用。
- 不猜测仪器协议、时间轴、TIC/EIC 语义、方法参数或谱库结果；模拟、公开示例、导入数据和真实设备证据必须明确区分。
- 客户数据、模型权重、构建缓存、本地工具链和安装包不得提交进 Git 历史。
- 客户拿独立安装包，工程师拿源码；源码编译通过不能替代最终安装包和真实目标系统验收。
